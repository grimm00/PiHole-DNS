# Optional overrides (copy to mise.local.toml — gitignored).
# Use if plain podman-compose in mise.toml is not on your PATH (e.g. distrobox wrapper):
#
# [tasks.up]
# run = "distrobox enter net-tools --env CONTAINER_HOST=unix:///run/user/1000/podman/podman.sock -- podman-compose -f docker-compose.yml -f docker-compose.desk.yml up -d"
